services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: nextread
      POSTGRES_USER: nextread
      POSTGRES_PASSWORD: localdev
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
